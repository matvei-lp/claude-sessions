# macOS
.DS_Store

# Local backups
*.bak
*.bak-*

# Per-machine alias symlinks (each machine recreates its own; data lives in -Users-venom-*)
-Users-mkozin-*

# Python cache
tools/__pycache__/

# Per-machine sync log (must not sync — would conflict on its own)
tools/sync.log
